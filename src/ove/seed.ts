import { demoData } from "../demo-config/active";

export const seedAssets = demoData.assets;
export const seedSkills = demoData.skills;
export const seedPeople = demoData.people;
export const seedTools = demoData.tools;
export const seedInventory = demoData.inventory;
export const seedProtocols = demoData.protocols;
export const seedSchedules = demoData.schedules;
export const seedReservations = demoData.reservations;
export const seedExecutions = demoData.executions;
export const seedIncidents = demoData.incidents;
export const seedNotifications = demoData.notifications;
export const seedOperationalFlows = demoData.operationalFlows;

export const plants = demoData.taxonomy.plants;
export const branches = demoData.taxonomy.branches;
export const operators = demoData.taxonomy.operators;
export const supervisors = demoData.taxonomy.supervisors;
export const categories = demoData.taxonomy.categories;
